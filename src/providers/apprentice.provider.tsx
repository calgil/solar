// import { createContext } from "react";
// import { MprType } from "../types/mpr.type";

// type ApprenticeContextType = {
//     mprs: MprType[]
// }

// const ApprenticeContext = createContext({} as ApprenticeContextType )

// export const ApprenticeProvider = ({children}: {children: React.ReactNode}) => {
// }
