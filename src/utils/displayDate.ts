// import { months } from "../data/months";

import { getMonthName } from "../data/months";

export const displayDate = (date: any) => {
  // console.log("stupid date", seconds);

  // const newDate = new Date(seconds * 1000);
  // const monthIndex = newDate.getMonth();
  // const year = newDate.getFullYear();
  // return `${getMonthName(monthIndex).name}, ${year}`;
  return "dates suck";
};
