export type Course = {
  id: string;
  // classId: string;
  name: string;
  link?: string;
  info?: string;
};

export type NewCourse = {
  // classId: string;
  name: string;
  link?: string;
  info?: string;
};
